import React from "react";
import styled from "styled-components";

const Sample = () => {
  return <MainWrapper>Sample</MainWrapper>;
};

export default Sample;

const MainWrapper = styled.div`
  width: 100%;
  height: 100%;
`;
