import React from "react";
import styled from "styled-components";

const NotFound = () => {
  return <MainWrapper>error 404 not found</MainWrapper>;
};

export default NotFound;

const MainWrapper = styled.div`
  width: 100%;
  height: 100%;
`;
